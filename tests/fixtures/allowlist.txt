# tests/invariants/test_i6_repo_scan.py が許すテスト用の偽の鍵・値。
# 実際のサービスに使える鍵ではない（テストのために作った文字列だけをここに書く）。

# .env.example のプレースホルダー
replace_with_your_api_key
# README.md の設定例のプレースホルダー
sk-orca-your-key
# web のコードで、変数や環境変数の名前を参照しているだけの箇所（値ではない）
session.access_token
process.env.WORKER_SHARED_SECRET
