# 反論台帳（decision-trace）

批評は Round 1（claude/claude-opus-5）と Round 2（codex/gpt-5.6-sol xhigh、3パートに分割）。v2・v3 の改訂は再批評を受けていない。

## 未解決

（なし）

## 解決済み（一行索引）

- C-1 管理者向けの組の質問で member に漏れる → v2 §5.3(7)：managers の組の質問は manager 宛てのみ、表示時に再確認（再批評なし）
- C-2 後からの非公開化の判定が引用だけを見ている → v2 §3・§5.3(2)(9)・§5.4・§5.5：input_segment_ids で判定、入力の絞り込みは実効の可視性だけ、回答の可視性は回答時点で決定。写り込みは P-1 へ分離（再批評なし）
- C-3 同名の再アップロードで他人のソースの版と可視性に届く → v2 §2.2・§3：再アップロードの単位を登録者ごとに、visibility の入力は無視（再批評なし）
- C-4 除外を member が戻せる／最後の manager を降格できる → v2 §1.3：除外の取り消しを制限、最後の manager の降格は 409（再批評なし）
- C-5 管理者限定の出来事が全員向けの決定を黙って置き換える → v2 §5.3(2)(4)(9)：置き換えは、置き換えた側が見える版でだけ有効（再批評なし）
- P-1 非公開化の前に別の出来事へ写った内容を追うか → ユーザー決定（2026-09-21）：追わない。design.md §5.5・§12、README に既知の制限として明記
- A-X1 メンバーを外す API が無い → v3 W7b を追加、所属は毎回 DB で確認（再批評なし）
- A-X2 JWT の検証項目が未定義 → v3 §1.2：alg を設定で固定、iss・aud・exp・nbf・role・sub を必須（再批評なし）
- A-X3 pid と子 ID を別々に引くと越境できる → v3 §1.3：親子を1つの条件で引く関数だけを authz.py に置く（再批評なし）
- A-X4 シークレットが未設定・空・同値でも起動する → v3 §1.2：起動時に32文字以上・相互不一致を検査、ヘッダー欠落は比較前に 401（再批評なし）
- A-X5 web の変更系に CSRF 対策が無い → v3 §2.7：Origin 照合・Content-Type 制限・Cookie 属性の固定（再批評なし）
- B-X1 managers の組の出来事が、根拠の公開化で全員向けに昇格する → v3 §5.3(2)・§5.5：partition=managers を実効の可視性の下限に、公開化したソースは全員向けの組で再抽出（再批評なし）
- B-X2 実行中の可視性変更で、送信時には非公開の内容を送る → v3 §3・§5.3(12)：visibility_epoch を送信直前に照合し、変わっていたら送らず作り直し（再批評なし）
- B-X3 W19 がクライアント指定の audience を信じうる → v3 §2.4：W19 は audience・レポートIDを入力に取らない（再批評なし）
- B-X4 無進捗の判定から管理者限定の進捗が推測できる → v3 §5.7：宛先の立場ごとに判定を分ける（再批評なし）
- B-X5 管理者向けの版が無い回に除外件数が出ない → v3 §5.6：manager にはどの版でも excluded_summary を返す（再批評なし）
- C-X1 コスト上限の確認と記録が原子的でない → v3 §6.1・§3：daily_costs の行ロックで最大コストを予約（再批評なし）
- C-X2 単一入口の検査が import だけ → v3 §6.1：静的検査を拡張し、上限到達状態で外向き通信ゼロを確かめる実行時テストを追加（再批評なし）
- C-X3 リポジトリの秘密を検査していない → v3 §8：git ls-files を redact.py の表で走査するテスト（ライブラリ追加なし）（再批評なし）
- C-X4 I3 のテストが TRUNCATE を試していない → v3 §8：全対象表で UPDATE・DELETE・TRUNCATE と行数の不変を確認（再批評なし）
- C-X5 I2 のテストが脚注しか見ていない → v3 §8：node_evidence・timeline の原文も完全一致を確認（再批評なし）
