critic: codex/gpt-5.6-sol xhigh（パートC：不変条件とテスト（§3.2・§6・§8）。抜粋のみ渡し、制限15分、完走）

## Round 2 — 2026-09-21 — 工程: 批評（クロスベンダー、design.md v2）
指摘IDは台帳では C-X1〜C-X5 と表記する

### [X-1]

- 種別：設計
- 深刻度：high
- 指摘：I7の事前確認とコスト記録が原子的でないため、並行実行時には上限超過後のLLM呼び出しを防げない。
- 具体的な破綻シナリオ：残額が0.10 USDのときworker Aが合計を読み取って停止し、worker Bが0.20 USDの呼び出しを完了・記録した後、Aが古い判定結果のままSDKを呼ぶと、`test_i7_cost.py`は通る一方で上限超過後に呼び出しが発生する。
- 直し方の提案：日次予算行のロックまたは原子的なコスト予約を導入し、2 workerをバリア同期させた競合テストを追加する。

### [X-2]

- 種別：設計
- 深刻度：high
- 指摘：I7の入口検査は特定importの探索だけなので、別のHTTPクライアントによる直接呼び出しを見逃す。
- 具体的な破綻シナリオ：新しい処理が`httpx.post()`でLLM互換エンドポイントを直接呼ぶと、`OrcaClient`も`openai`もimportしないため`test_i7_single_entry.py`は通るが、コスト上限を迂回できる。
- 直し方の提案：LLM資格情報を専用ゲートウェイだけに与える能力境界を設け、静的検査に加えて上限到達状態で全公開処理を実行し、外向き通信がゼロであることを検査する。

### [X-3]

- 種別：設計
- 深刻度：high
- 指摘：I6のテストにはリポジトリ内の秘密情報を検出する検査がなく、伏せ字処理が正しくても条件の前半を保証できない。
- 具体的な破綻シナリオ：実キーを設定例やテストfixtureへ誤ってコミットしても、代表的な文字列のredactionと偽LLM入力の検査はすべて通る。
- 直し方の提案：CIでGit管理対象だけを列挙し、`.env*`の追跡禁止、既知prefix、資格情報形式、エントロピーをallowlist付きで検査する。

### [X-4]

- 種別：実装
- 深刻度：high
- 指摘：I3のテストは`TRUNCATE`を試しておらず、設計に明記された権限剥奪の欠落を検出できない。
- 具体的な破綻シナリオ：migrationの退行で`app_worker`に`TRUNCATE`が残ると、UPDATE・DELETEトリガーを起動せず`source_audit_log`などを全消去できるが、現行テストは通る。
- 直し方の提案：保護対象の全テーブルについて実DBの`app_worker`としてUPDATE・DELETE・TRUNCATEを個別に試し、拒否と既存行数の不変を確認する。

### [X-5]

- 種別：設計
- 深刻度：mid
- 指摘：I2のテストは`footnotes.text`だけを比較しており、設計上対象になっている図中の原文表示を確かめていない。
- 具体的な破綻シナリオ：保存原文が「採用日は4月1日」なのに、図のラベルやtooltipだけLLM出力の「採用日は4月2日」を使い、脚注のみDB原文へ差し替える実装ならテストは通る。
- 直し方の提案：図、tooltip、`node_evidence`を含む全引用表示をsegment IDから逆引きし、DB本文との完全一致をAPIとブラウザ表示の両方で検査する。

I1・I4・I5については、C-1〜C-5およびP-1を除外した範囲では、記載されたテストに新たなすり抜けを認めませんでした。